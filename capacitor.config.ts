import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.tap.jacking',
  appName: 'cs-tap-jacking',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
